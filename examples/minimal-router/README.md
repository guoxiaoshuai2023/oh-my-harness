# Minimal Router Example

This is the smallest downstream router shape for the unified adaptive Harness. It keeps hard gates and trigger routes visible while leaving capability selection, main-only lifecycle, hard no-fan-out, Technical Solution Design, evaluator reports, and finite failure control in the routed protocols.

Use it when a target repository wants trigger-based governance without copying the full source documentation into AGENTS.md.
