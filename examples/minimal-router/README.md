# Minimal Router Example

This example shows the smallest downstream `AGENTS.md` router shape.

Use it when a target repo does not yet need the full route stack but should keep the same trigger-based architecture.
